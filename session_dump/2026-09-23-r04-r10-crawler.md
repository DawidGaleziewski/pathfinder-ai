# Session dump: 2026-09-23, R-04 to R-10 (crawler map mode)

Branch `feature/001-r04-core-crawler-map-mode`. Committed per roadmap item (see `roadmap.md` Commits, `git log`). Read this, then `roadmap.md` and
`specs/001-crawler-map-mode/tasks.md` (the source of truth for status).

## State at end of session
- 70 of 74 tasks `[X]`. Open: **T059** (verify placeholder obstacle selectors on a live run), **T072** (manual
  compliance gate), **T073** (quickstart on the live portal), **T074** (live bypass-attempt run; static review done).
- `pnpm --dir apps/crawler`: 345 tests pass (38 files), typecheck and eslint clean. `prettier --check` fails only on
  `eslint.config.mjs` (pre-existing).
- Roadmap: R-04, R-05, R-06, R-08, R-09 done; R-07 and R-10 in progress. `CHANGELOG.md` untouched.
- Real server verified over stdio via `.mcp.json`: 8 agent tools; the real Allegro portal is refused (null compliance,
  placeholder User-Agent), as designed.

## What was built (by package, `apps/crawler/packages/`)
- **core**: Zod schemas (`schemas/`, incl. `action`, `decision_log`), `db.ts` (better-sqlite3 + Kysely, PRAGMAs on
  connection), `migrate.ts` (`NNNN_name.up/down.sql`, `schema_migrations`), `ids.ts` (UUIDv7, `nowIso`), `pii.ts`
  (`maskText`, `scrubJson`, `looksLikeRawPayload`), `evidence.ts` (sha256 store, masks before writing), `log.ts`
  (pino to stderr + `decision_log` table), `audit.ts` (`auditPii`, `compareRuns`).
- **config**: `portal-schema`, `persona-schema`, `load-portal`, `load-persona` (depth-first `extends`, objects merge,
  arrays replace, cycle and inline-secret rejection), `effective` (`min(portal, persona)`), `ConfigError`.
- **safety**: `rules.ts` (keyword and path data, PL+EN), `classifier` (`classifyAction`, `classifyUrl`; unknown means
  non-read; a claimed class can only raise), `scope`, `denylist`, `env-guard`, `block-detector`.
- **crawler**: `action-gate.decide` (classifier, scope, denylist, ceiling, budgets), `rate-limiter`, `request-gate`
  (UA, limiter, block stop, main-frame navigation policy), `preflight`, `session` (only Playwright instance),
  `stabilizer`, `observer` (ARIA, forms without values, test ids), `network-recorder` (shapes only), `action-extractor`,
  `locators`, `frontier` (policy plus queue persistence), `report`, `shape`.
- **obstacles**: `registerObstacleHandlers` with `sweep()`; handlers are paused during a sweep.
- **mcp-server**: `services/` (record_state/form/transition/api_call, agent notes, frontier, complete/finish_run,
  start_run record, run lifecycle), `tools/` (8 agent tools, `AGENT_TOOL_NAMES`), `runtime/` (`pipeline.ts` for
  navigate and act, `browser-runtime.ts`, `run-state.ts`), `server.ts`, `main.ts`.
- Repo: `portals/allegro-lokalnie/portal.yaml`, `personas/_mixins/anonymous-base.yaml`,
  `personas/allegro-lokalnie/{guest,guest-mobile}.yaml`, `.claude/agents/crawler.md`, `.mcp.json`, `README.md`,
  `apps/crawler/scripts/{pii-audit,stability-check}.ts` (`pnpm audit:pii`, `pnpm stability`).

## Decisions made (not all in the original spec)
- The server records into `data/db/<PATHFINDER_ENV>.sqlite` (default `production`); portals of another environment
  are refused with `ENV_GUARD_REFUSED`.
- New contract items: portal `block_signatures`; error codes `SAFETY_CLASS_MISMATCH` and `UNKNOWN_REF` (also for
  `add_open_question`); action `nth` and `snapshot_ref`.
- Frontier is one table: `pending`, `done`, plus five skip statuses. Decision log is a table, mirrored by pino.
  Timestamps are ISO text with ms, ids UUIDv7, `runs.mode` has no CHECK.
- `act` on an action from another state re-navigates to its `page_url` through the same gates.
- Budget exhaustion completes the run then returns `RUN_STOPPED`; stale `running` runs become `interrupted` on
  server boot and shutdown (`interruptStaleRuns`).
- Fingerprint index is rebuilt at session open from stored masked snapshots, in creation order.
- pnpm: `esbuild: false` in `pnpm-workspace.yaml` (denied, tsx works); root depends on `@pathfinder/core` for scripts.

## Environment notes
- Chromium is installed but fails here: missing `libnspr4`, `libnss3`, `libasound2` and no sudo. Workaround used:
  `.deb`s extracted to the session scratchpad plus `LD_LIBRARY_PATH` (gone with the session). Permanent fix:
  `pnpm --dir apps/crawler exec playwright install --with-deps chromium` (needs sudo). Browser tests skip themselves
  without a browser; the integration file `mcp-server/tests/map-run.test.ts` takes about 60 s.
- Lessons: Playwright `addLocatorHandler` intercepts a click on its own locator (2 s stall); prettier reformats code
  so scripted string replacements need re-checking; `sleep` chains are blocked, use Monitor or background runs.

## Gaps / next session
1. Branch is committed per item; `po` to review, merge and close R-07/R-10 after T059/T072-T074.
2. T072 (person): fill `compliance.*` and the real contact address in the portal file.
3. T059/T073/T074: supervised live run, quickstart 1 to 9, then the "ignore the guards" prompt.
4. Known limits: `frontier.state_id` NOT NULL (a refusal before any state is only in the decision log); route
   templates re-inferred after restart; consent flags only deny permissions; no clickable divs or typing; any 403/429,
   even on a subresource, stops the run; cluster ids depend on creation order.
5. Optional: `pnpm approve-builds` decision for esbuild; prettier on `eslint.config.mjs`.
